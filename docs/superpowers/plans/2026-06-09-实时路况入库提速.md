# 实时路况入库提速 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把全量实时路况（约 40 万行/2 分钟）入库从分钟级降到秒级，并提供 Redis 热读 SDK 让第三方按 link 查到永远最新的路况。

**Architecture:** DB 端双方言 executemany 批量 upsert + SQLite PRAGMA；Redis 端 mget/mset 批量化（修掉 incremental/snapshot 逐行往返）；incremental 减量；新增 `TrafficReader`（Redis 命中→DB 回落）。SQLite/MySQL 双引擎。

**Tech Stack:** Python 3.11、SQLAlchemy Core、Redis(fakeredis 测)、pytest。

**环境（实现者必读）：** 解释器只有 `.venv/bin/python`（无 `python`/`pytest` on PATH）。一律 `.venv/bin/python -m pytest ...`。直接在 `main` 分支工作（用户已授权），git 身份已配置。

**Spec:** `docs/superpowers/specs/2026-06-09-实时路况入库提速-design.md`

---

## File Structure

- Modify: `amap_service/db/repositories.py` — 重写 `upsert_traffic_status` 为批量 executemany，+ `_traffic_bulk_upsert` 方言辅助；返回 `{written, failed}`。(不动 `upsert_road_links` / `_upsert_stmt`。)
- Modify: `amap_service/db/engine.py` — SQLite 连接 PRAGMA。
- Modify: `amap_service/cache/client.py` — `RedisCache`/`NoOpCache` 增加 `mget`/`mset`。
- Modify: `amap_service/pipelines/traffic.py` — incremental/snapshot 改 mget/mset 批量。
- Create: `amap_service/sdk/traffic_query.py` — `TrafficReader`。
- Modify: `amap_service/sdk/__init__.py` — 导出 `TrafficReader`。
- Modify: `config/config.yaml.example`、`docs/命令说明.md`。
- Tests: `tests/db/test_repositories.py`（改）、`tests/db/test_engine.py`（增）、`tests/cache/test_client.py`（增）、`tests/pipelines/test_traffic.py`（改/增）、`tests/sdk/test_traffic_query.py`（新）。

---

## Task 1: DB 批量 upsert（核心提速）

**Files:**
- Modify: `amap_service/db/repositories.py`
- Test: `tests/db/test_repositories.py`、`tests/pipelines/test_traffic.py`（同步改断言）

- [ ] **Step 1: 改测试（新契约 = 失败）**

1a. 在 `tests/db/test_repositories.py` 中，把 `test_traffic_upsert_latest_only` 的两处 stats 断言改为新返回键，并新增一个多批次大批量测试。替换该函数为：
```python
def test_traffic_upsert_latest_only(tmp_path):
    e = _engine(tmp_path)
    rid = 5130516143645130888
    s1 = upsert_traffic_status(e, [{"link_id": rid, "speed": 89, "state": 1, "travel_time": 59,
                                    "traffic_time": "2026-05-18 14:51:04"}])
    assert s1["written"] == 1 and s1["failed"] == 0
    s2 = upsert_traffic_status(e, [{"link_id": rid, "speed": 40, "state": 3, "travel_time": 120,
                                    "traffic_time": "2026-05-18 14:53:04"}])
    assert s2["written"] == 1 and s2["failed"] == 0

    with e.connect() as c:
        assert c.execute(select(func.count()).select_from(traffic_status)).scalar() == 1
        row = c.execute(
            select(traffic_status.c.speed, traffic_status.c.state, traffic_status.c.travel_time,
                   traffic_status.c.traffic_time)
        ).one()
        assert tuple(row) == (40, 3, 120, "2026-05-18 14:53:04")   # latest值含路况时间


def test_traffic_upsert_bulk_multibatch(tmp_path):
    e = _engine(tmp_path)
    rows = [{"link_id": 1000 + i, "speed": i % 100, "state": (i % 4) + 1,
             "travel_time": i, "traffic_time": "2026-05-18 14:51:04"} for i in range(12000)]
    s = upsert_traffic_status(e, rows, batch_size=5000)   # 跨 3 批
    assert s["written"] == 12000 and s["failed"] == 0
    with e.connect() as c:
        assert c.execute(select(func.count()).select_from(traffic_status)).scalar() == 12000
    # 重跑覆盖：行数不变，值刷新
    rows2 = [{"link_id": 1000 + i, "speed": 7, "state": 2, "travel_time": 0,
              "traffic_time": "2026-05-18 15:00:00"} for i in range(12000)]
    s2 = upsert_traffic_status(e, rows2, batch_size=5000)
    assert s2["written"] == 12000
    with e.connect() as c:
        assert c.execute(select(func.count()).select_from(traffic_status)).scalar() == 12000
        assert c.execute(select(traffic_status.c.speed).where(traffic_status.c.link_id == 1000)).scalar() == 7
```

1b. 在 `tests/pipelines/test_traffic.py` 中，把 `test_run_traffic_both_modes` 里的 `assert stats["inserted"] == 2 and stats["failed"] == 0` 改为：
```python
    assert stats["written"] == 2 and stats["failed"] == 0
```
（该文件其它测试不读 stats 键，无需改。）

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/db/test_repositories.py -k traffic -v`
Expected: FAIL（旧实现返回 inserted/updated，新断言读 written）。

- [ ] **Step 3: 重写实现**

在 `amap_service/db/repositories.py` 中，整体替换 `upsert_traffic_status`（保留 `_upsert_stmt`、`upsert_road_links`、`_upsert_one_link` 不动），并新增 `_traffic_bulk_upsert` 辅助。`sqlite_insert`、`mysql_insert`、`func` 已在文件顶部导入。

```python
def _traffic_bulk_upsert(conn):
    """方言相关的批量 upsert 语句（不带 values，配 executemany 参数列表）。
    更新列取自新值；updated_at 用 DB 端 current_timestamp()。"""
    cols = ["speed", "state", "travel_time", "traffic_time"]
    name = conn.dialect.name
    if name == "sqlite":
        stmt = sqlite_insert(traffic_status)
        set_ = {c: getattr(stmt.excluded, c) for c in cols}
        set_["updated_at"] = func.current_timestamp()
        return stmt.on_conflict_do_update(index_elements=["link_id"], set_=set_)
    if name == "mysql":
        stmt = mysql_insert(traffic_status)
        set_ = {c: getattr(stmt.inserted, c) for c in cols}
        set_["updated_at"] = func.current_timestamp()
        return stmt.on_duplicate_key_update(**set_)
    raise ValueError(f"unsupported dialect for upsert: {name}")


def upsert_traffic_status(engine: Engine, rows: Iterable[dict], batch_size: int = 5000) -> dict:
    """批量 upsert traffic_status（按 link_id，latest-only），刷新 updated_at。

    双方言 executemany：每批一条 INSERT...ON CONFLICT/DUPLICATE UPDATE，配参数列表。
    返回 {written, failed}（latest-only 不区分 inserted/updated）。"""
    stats = {"written": 0, "failed": 0}
    for batch in _batched(rows, batch_size):
        params = [{"link_id": r["link_id"], "speed": r.get("speed"), "state": r.get("state"),
                   "travel_time": r.get("travel_time"), "traffic_time": r.get("traffic_time")}
                  for r in batch]
        if not params:
            continue
        try:
            with engine.begin() as conn:
                conn.execute(_traffic_bulk_upsert(conn), params)
            stats["written"] += len(params)
        except Exception:  # noqa: BLE001
            stats["failed"] += len(batch)
            logger.exception("traffic_status bulk batch failed (%d rows)", len(batch))
    return stats
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/db/test_repositories.py tests/pipelines/test_traffic.py -v`
Expected: PASS（含新的 bulk_multibatch）。
Run: `.venv/bin/python -m pytest -q`
Expected: 全量 PASS。

- [ ] **Step 5: Commit**

```bash
git add amap_service/db/repositories.py tests/db/test_repositories.py tests/pipelines/test_traffic.py
git commit -m "perf(db): traffic_status 批量 executemany upsert(分钟->秒) + 返回 {written,failed}"
```

---

## Task 2: SQLite PRAGMA

**Files:**
- Modify: `amap_service/db/engine.py`
- Test: `tests/db/test_engine.py`

- [ ] **Step 1: Write the failing test**

先看 `tests/db/test_engine.py` 现有内容并在其风格上追加。追加：
```python
from sqlalchemy import text
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine


def test_sqlite_pragmas_applied(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    with e.connect() as c:
        assert c.execute(text("PRAGMA journal_mode")).scalar().lower() == "wal"
        assert int(c.execute(text("PRAGMA synchronous")).scalar()) == 1   # NORMAL
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/db/test_engine.py -k pragmas -v`
Expected: FAIL（journal_mode 默认 delete/memory，非 wal）。

- [ ] **Step 3: Write minimal implementation**

替换 `amap_service/db/engine.py` 的 `make_engine`，对 sqlite 引擎挂 connect 事件设 PRAGMA：
```python
from sqlalchemy import Engine, create_engine, event

from amap_service.config.schema import DatabaseConfig


def build_url(db: DatabaseConfig) -> str:
    if db.type == "sqlite":
        return f"sqlite:///{db.sqlite.path}"
    if db.type == "mysql":
        m = db.mysql
        return (
            f"mysql+pymysql://{m.user}:{m.password}@{m.host}:{m.port}/{m.database}"
            f"?charset={m.charset}"
        )
    raise ValueError(f"unsupported database type: {db.type}")


def _apply_sqlite_pragmas(dbapi_conn, _record):
    cur = dbapi_conn.cursor()
    cur.execute("PRAGMA journal_mode=WAL")
    cur.execute("PRAGMA synchronous=NORMAL")
    cur.execute("PRAGMA temp_store=MEMORY")
    cur.execute("PRAGMA cache_size=-65536")  # ~64MB page cache
    cur.close()


def make_engine(db: DatabaseConfig) -> Engine:
    engine = create_engine(build_url(db), future=True)
    if db.type == "sqlite":
        event.listen(engine, "connect", _apply_sqlite_pragmas)
    return engine
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/db/test_engine.py -v`
Expected: PASS.
Run: `.venv/bin/python -m pytest -q`
Expected: 全量 PASS（注意：WAL 会在库旁生成 -wal/-shm 文件，测试用 tmp_path 不受影响）。

- [ ] **Step 5: Commit**

```bash
git add amap_service/db/engine.py tests/db/test_engine.py
git commit -m "perf(db): SQLite 连接启用 WAL/synchronous=NORMAL 等 PRAGMA"
```

---

## Task 3: 缓存批量接口 mget/mset

**Files:**
- Modify: `amap_service/cache/client.py`
- Test: `tests/cache/test_client.py`

- [ ] **Step 1: Write the failing test**

追加到 `tests/cache/test_client.py`：
```python
def test_redis_cache_mset_mget_roundtrip():
    c = RedisCache(fakeredis.FakeRedis())
    c.mset({"a": "1", "b": "2"})
    assert c.mget(["a", "b", "missing"]) == ["1", "2", None]
    assert c.mget([]) == []


def test_redis_cache_mset_empty_noop():
    c = RedisCache(fakeredis.FakeRedis())
    c.mset({})            # 不报错
    assert c.mget(["x"]) == [None]


def test_redis_cache_mset_ttl():
    r = fakeredis.FakeRedis()
    c = RedisCache(r)
    c.mset({"k": "v"}, ttl=100)
    assert c.get("k") == "v"
    assert r.ttl("k") > 0


def test_noop_cache_batch_inert():
    c = NoOpCache()
    assert c.mget(["a", "b"]) == [None, None]
    c.mset({"a": "1"})    # 不报错
    assert c.mget(["a"]) == [None]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/cache/test_client.py -k "mset or mget or batch" -v`
Expected: FAIL（无 mget/mset）。

- [ ] **Step 3: Write minimal implementation**

在 `amap_service/cache/client.py` 中给两个类加方法。`NoOpCache`：
```python
    def mget(self, keys: list) -> list:
        return [None] * len(keys)

    def mset(self, mapping: dict, ttl: Optional[int] = None) -> None:
        pass
```
`RedisCache`：
```python
    def mget(self, keys: list) -> list:
        if not keys:
            return []
        vals = self._r.mget(keys)
        return [v.decode() if isinstance(v, (bytes, bytearray)) else v for v in vals]

    def mset(self, mapping: dict, ttl: Optional[int] = None) -> None:
        if not mapping:
            return
        if ttl:
            pipe = self._r.pipeline()
            for k, v in mapping.items():
                pipe.setex(k, ttl, v)
            pipe.execute()
        else:
            self._r.mset(mapping)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/cache/test_client.py -v`
Expected: PASS.
Run: `.venv/bin/python -m pytest -q`
Expected: 全量 PASS。

- [ ] **Step 5: Commit**

```bash
git add amap_service/cache/client.py tests/cache/test_client.py
git commit -m "feat(cache): RedisCache/NoOpCache 增加 mget/mset 批量接口"
```

---

## Task 4: 路况管线 Redis 路径批量化

**Files:**
- Modify: `amap_service/pipelines/traffic.py`
- Test: `tests/pipelines/test_traffic.py`

- [ ] **Step 1: Write the failing test**

追加到 `tests/pipelines/test_traffic.py`（顶部已 import httpx/HttpClient/run_traffic/select/func/traffic_status；新增 import）：
```python
import json as _json
import fakeredis
from amap_service.cache.client import RedisCache


def _client_for(payload):
    def handler(request):
        return httpx.Response(200, json=payload)
    return HttpClient(backoff_seconds=0, transport=httpx.MockTransport(handler))


def test_run_traffic_incremental_only_writes_changed(tmp_path):
    e = _engine(tmp_path)
    cache = RedisCache(fakeredis.FakeRedis())
    payload = {"linkStates": [
        {"linkId": 1, "speed": 80, "state": 1, "travelTime": 10},
        {"linkId": 2, "speed": 50, "state": 2, "travelTime": 20},
    ]}
    # 首跑：两条都算变化，都写
    c1 = _client_for(payload)
    s1 = run_traffic(e, c1, "http://h", "/g5_server/map/api/traffic/status",
                     cache=cache, incremental=True, snapshot=True)
    c1.close()
    assert s1["written"] == 2
    # 次跑：同样 payload，签名都未变 -> 0 写
    c2 = _client_for(payload)
    s2 = run_traffic(e, c2, "http://h", "/g5_server/map/api/traffic/status",
                     cache=cache, incremental=True, snapshot=True)
    c2.close()
    assert s2["written"] == 0
    # snapshot 热缓存可读
    assert _json.loads(cache.get("traffic:latest:1"))["speed"] == 80


def test_run_traffic_incremental_detects_change(tmp_path):
    e = _engine(tmp_path)
    cache = RedisCache(fakeredis.FakeRedis())
    c1 = _client_for({"linkStates": [{"linkId": 1, "speed": 80, "state": 1, "travelTime": 10}]})
    run_traffic(e, c1, "http://h", "/g5_server/map/api/traffic/status",
                cache=cache, incremental=True); c1.close()
    c2 = _client_for({"linkStates": [{"linkId": 1, "speed": 20, "state": 3, "travelTime": 40}]})
    s = run_traffic(e, c2, "http://h", "/g5_server/map/api/traffic/status",
                    cache=cache, incremental=True); c2.close()
    assert s["written"] == 1
    with e.connect() as c:
        assert tuple(c.execute(select(traffic_status.c.speed, traffic_status.c.state)
                               .where(traffic_status.c.link_id == 1)).one()) == (20, 3)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/pipelines/test_traffic.py -k "incremental" -v`
Expected: 多半 FAIL 或不稳定（旧逐行 get/set 仍工作但 stats 现在是 written；若旧实现已返回 written 则可能部分通过——以实现为准）。无论如何 Step 3 把它改成批量后必须稳定通过。

- [ ] **Step 3: Write minimal implementation**

替换 `amap_service/pipelines/traffic.py` 中 `run_traffic` 的缓存分支为 mget/mset 批量（`_signature` 与函数签名保持不变）：
```python
    use_cache = cache is not None and getattr(cache, "enabled", False) and (snapshot or incremental)
    if not use_cache:
        stats = upsert_traffic_status(engine, rows)
        logger.info("traffic: done %s", stats)
        return stats

    rows = list(rows)  # cache path needs multiple passes
    if incremental:
        sig_keys = [f"traffic:sig:{r['link_id']}" for r in rows]
        old_sigs = cache.mget(sig_keys)
        changed, new_sigs = [], {}
        for row, old in zip(rows, old_sigs):
            sig = _signature(row)
            if old != sig:
                changed.append(row)
                new_sigs[f"traffic:sig:{row['link_id']}"] = sig
        if new_sigs:
            cache.mset(new_sigs)
        rows = changed

    stats = upsert_traffic_status(engine, rows)

    if snapshot and rows:
        cache.mset({f"traffic:latest:{row['link_id']}": json.dumps(row) for row in rows})

    logger.info("traffic: done %s (cached: snapshot=%s incremental=%s)", stats, snapshot, incremental)
    return stats
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/pipelines/test_traffic.py -v`
Expected: PASS（含新增 incremental 用例 + 原有 both_modes/refresh）。
Run: `.venv/bin/python -m pytest -q`
Expected: 全量 PASS。

- [ ] **Step 5: Commit**

```bash
git add amap_service/pipelines/traffic.py tests/pipelines/test_traffic.py
git commit -m "perf(pipeline): 路况 incremental/snapshot 改 mget/mset 批量"
```

---

## Task 5: TrafficReader 热读 SDK

**Files:**
- Create: `amap_service/sdk/traffic_query.py`
- Modify: `amap_service/sdk/__init__.py`
- Test: `tests/sdk/test_traffic_query.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/sdk/test_traffic_query.py
import json
import fakeredis
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.cache.client import RedisCache, NoOpCache
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db
from amap_service.db.repositories import upsert_traffic_status
from amap_service.sdk import TrafficReader


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def test_get_latest_traffic_redis_hit_and_db_fallback(tmp_path):
    e = _engine(tmp_path)
    # DB 有 link 1、2；Redis 只有 link 1（且值与 DB 不同，验证优先用 Redis）
    upsert_traffic_status(e, [
        {"link_id": 1, "speed": 10, "state": 1, "travel_time": 5, "traffic_time": "t1"},
        {"link_id": 2, "speed": 20, "state": 2, "travel_time": 6, "traffic_time": "t2"},
    ])
    cache = RedisCache(fakeredis.FakeRedis())
    cache.set("traffic:latest:1", json.dumps(
        {"link_id": 1, "speed": 99, "state": 3, "travel_time": 7, "traffic_time": "t9"}))
    out = TrafficReader(e, cache).get_latest_traffic([1, 2, 3])
    assert out[1] == {"state": 3, "speed": 99, "travel_time": 7, "traffic_time": "t9"}  # Redis 优先
    assert out[2] == {"state": 2, "speed": 20, "travel_time": 6, "traffic_time": "t2"}  # DB 回落
    assert 3 not in out                                                                  # 两处都无


def test_get_latest_traffic_no_cache_pure_db(tmp_path):
    e = _engine(tmp_path)
    upsert_traffic_status(e, [{"link_id": 1, "speed": 10, "state": 1,
                               "travel_time": 5, "traffic_time": "t1"}])
    r = TrafficReader(e)                       # cache=None
    assert r.get_latest_traffic([1])[1]["state"] == 1
    r2 = TrafficReader(e, NoOpCache())         # 禁用缓存
    assert r2.get_latest_traffic([1])[1]["state"] == 1
    assert r.get_latest_traffic([]) == {}


def test_get_latest_traffic_dedups_ids(tmp_path):
    e = _engine(tmp_path)
    upsert_traffic_status(e, [{"link_id": 1, "speed": 10, "state": 1,
                               "travel_time": 5, "traffic_time": "t1"}])
    assert list(TrafficReader(e).get_latest_traffic([1, 1, 1]).keys()) == [1]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/sdk/test_traffic_query.py -v`
Expected: FAIL（cannot import TrafficReader）。

- [ ] **Step 3: Write minimal implementation**

3a. Create `amap_service/sdk/traffic_query.py`:
```python
"""查询层 SDK：按 link_id 取实时路况，先 Redis(traffic:latest:{id}) 命中、未命中回落 DB。"""
import json

from sqlalchemy import Engine, select

from amap_service.db.schema import traffic_status


def _row(d: dict) -> dict:
    return {"state": d.get("state"), "speed": d.get("speed"),
            "travel_time": d.get("travel_time"), "traffic_time": d.get("traffic_time")}


class TrafficReader:
    def __init__(self, engine: Engine, cache=None):
        self.engine = engine
        self.cache = cache

    def get_latest_traffic(self, link_ids: list) -> dict:
        """-> { link_id: {state, speed, travel_time, traffic_time} }。
        Redis 命中优先；未命中的一次 IN 查 DB 回落；两处都无的 link 不出现在结果里。"""
        ids = list(dict.fromkeys(link_ids))   # 去重保序
        if not ids:
            return {}
        result, misses = {}, ids
        if self.cache is not None and getattr(self.cache, "enabled", False):
            vals = self.cache.mget([f"traffic:latest:{i}" for i in ids])
            misses = []
            for i, v in zip(ids, vals):
                if v is None:
                    misses.append(i)
                else:
                    result[i] = _row(json.loads(v))
        if misses:
            with self.engine.connect() as conn:
                rows = conn.execute(
                    select(traffic_status.c.link_id, traffic_status.c.state,
                           traffic_status.c.speed, traffic_status.c.travel_time,
                           traffic_status.c.traffic_time)
                    .where(traffic_status.c.link_id.in_(set(misses)))
                ).all()
            for r in rows:
                result[r.link_id] = {"state": r.state, "speed": r.speed,
                                     "travel_time": r.travel_time, "traffic_time": r.traffic_time}
        return result
```

3b. 在 `amap_service/sdk/__init__.py` 增加导出（保留现有 LinkInfo/TrackConverter/StationTrafficResolver）：
```python
from amap_service.sdk.traffic_query import TrafficReader
```
并把 `TrafficReader` 加入 `__all__`。

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/sdk/test_traffic_query.py -v`
Expected: PASS (3 tests)。
Run: `.venv/bin/python -m pytest -q`
Expected: 全量 PASS。

- [ ] **Step 5: Commit**

```bash
git add amap_service/sdk/traffic_query.py amap_service/sdk/__init__.py tests/sdk/test_traffic_query.py
git commit -m "feat(sdk): TrafficReader 实时路况热读(Redis 命中->DB 回落)"
```

---

## Task 6: 配置样例 + 文档 + 全量回归

**Files:**
- Modify: `config/config.yaml.example`
- Modify: `docs/命令说明.md`

- [ ] **Step 1: 配置样例**

读 `config/config.yaml.example` 的 `redis:` 块。把 `redis.uses` 下的 `incremental_detection` 与 `latest_traffic_snapshot` 改/确保为 `true`（若该块字段不同，按实际字段名设置；找不到则在 `redis.uses` 下补这两行，缩进对齐）：
```yaml
  uses:
    incremental_detection: true     # 只把变化的 link 写库（配合 Redis 减量）
    latest_traffic_snapshot: true   # 写 traffic:latest:{id} 供 TrafficReader 热读
```
（保持 `enabled` 现状；不动其它块。）

- [ ] **Step 2: 文档**

在 `docs/命令说明.md` 的实时路况一节（第 2 节）补一段说明（匹配文件 markdown 风格）：
```markdown
> 提速与实时供数：`traffic` 入库走批量 upsert（40 万行秒级）。开启 `redis.uses.incremental_detection`
> 只写变化的 link、`latest_traffic_snapshot` 写热缓存；第三方按 link 查实时路况请用 SDK 的
> `TrafficReader.get_latest_traffic(link_ids)`（Redis 命中→DB 回落，永远是最后一次抓取值）。
```

- [ ] **Step 3: 全量回归**

Run: `.venv/bin/python -m pytest -q`
Expected: 全部通过。

- [ ] **Step 4: Commit**

```bash
git add config/config.yaml.example docs/命令说明.md
git commit -m "docs+config: 开启路况 Redis 减量/热缓存 + TrafficReader 用法说明"
```

---

## Self-Review

**1. Spec coverage**
- §3.A DB 批量 upsert + 返回 {written,failed} → Task 1。PRAGMA → Task 2。
- §3.B Redis mget/mset → Task 3；incremental/snapshot 批量化 → Task 4。
- §3.C incremental 默认开 → Task 6（配置）。
- §3.D TrafficReader（Redis 命中→DB 回落、缺失不出现）→ Task 5。
- §3.E 双引擎：`_traffic_bulk_upsert` 按方言分支（Task 1）；PRAGMA 仅 sqlite（Task 2）；MySQL 分支至少由方言选择逻辑覆盖（Task 1 的 sqlite 路径全链路测，mysql 路径为语句构造分支，不依赖真实 MySQL）。
- §4 不做项（staging/不动 section 叠加/无历史/无 HTTP API）→ 计划未引入。
- §5 测试矩阵 → 各任务 test 齐备。§6 落地配置/文档 → Task 6。

**2. Placeholder scan**：除 Task 2/6 明确要求"按现有文件风格/实际字段名适配"外（engine/config/docs 的既有结构需现场对齐），其余步骤均给完整代码 + 确切命令 + 预期。无 TBD。

**3. Type/consistency**：`upsert_traffic_status(engine, rows, batch_size=5000) -> {written, failed}`（Task 1）被 `run_traffic`（Task 4）直接返回，pipeline 测试断言已同步改为 `written`（Task 1 Step 1b）。`cache.mget(keys)->list` / `cache.mset(mapping, ttl=None)`（Task 3）被 Task 4 的 incremental/snapshot 调用，签名一致。`TrafficReader.get_latest_traffic(link_ids)->{id:{state,speed,travel_time,traffic_time}}`（Task 5）读取 snapshot 写入的 `json.dumps(row)`（row 含 speed/state/travel_time/traffic_time，Task 4），键对齐。snapshot 键 `traffic:latest:{id}` 在 Task 4（写）与 Task 5（读）一致；签名键 `traffic:sig:{id}` 仅 Task 4 内部使用。
