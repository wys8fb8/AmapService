# 站间路况分段 v2 静态化重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把静态的站间路段占比物化到 `transit_section_link` 表（随 build 重算），查询层 SDK 改为「线名进、查表 + 叠实时路况」；新增 `transit_station` 表与独立 `section-build` 步骤。

**Architecture:** 三层 —— transit-build 写 `transit_segment` + `transit_station`；section-build 纯 DB→DB 跑几何算法写 `transit_section_link`（无路况）；查询层 `StationTrafficResolver` 查静态表 + 一次路况 join。纯几何函数迁到 `sdk/section_compute.py`，`sdk/station_traffic.py` 瘦身为读取器。

**Tech Stack:** Python 3.11、SQLAlchemy Core、pytest。

**环境（实现者必读）：** 解释器只有 `.venv/bin/python`（无 `python`/`pytest` on PATH）。一律 `.venv/bin/python -m pytest ...`。直接在 `main` 分支工作（用户已授权），git 身份已配置。

**Spec:** `docs/superpowers/specs/2026-06-08-站间路况分段-v2-静态化重构-design.md`

---

## File Structure

- Modify: `amap_service/db/schema.py` — + `transit_station`、`transit_section_link` 表
- Modify: `amap_service/db/repositories.py` — + `replace_transit_stations`、`replace_transit_section_links`
- Modify: `amap_service/parsing/transit.py` — + `parse_line_stations`
- Modify: `amap_service/config/schema.py` — + `SdkConfig.section_sample_step_m`
- Create: `amap_service/sdk/section_compute.py` — 纯几何 + `compute_section_rows`（从 station_traffic 迁入）
- Modify: `amap_service/sdk/station_traffic.py` — 先改为从 section_compute re-export（Task 4），后瘦身为读取器（Task 7）
- Modify: `amap_service/sdk/__init__.py` — 导出调整
- Create: `amap_service/pipelines/section_build.py` — `run_section_build`
- Modify: `amap_service/pipelines/transit_build.py` — 写 station + 末尾触发 section-build
- Modify: `amap_service/cli.py` — `run-once section-build`
- Create/Modify tests: `tests/sdk/test_section_compute.py`、`tests/sdk/test_station_traffic.py`（重写）、`tests/pipelines/test_section_build.py`、`tests/parsing/test_transit.py`、`tests/db/test_transit_station.py`、`tests/db/test_section_link.py`

排序保证每个提交全量测试绿：Task 4 让 station_traffic 从 section_compute re-export（v1 测试仍过），Task 7 才重写读取器与其测试。

---

## Task 1: 新增两张表

**Files:**
- Modify: `amap_service/db/schema.py`
- Test: `tests/db/test_section_link.py`（新）

- [ ] **Step 1: Write the failing test**

```python
# tests/db/test_section_link.py
from sqlalchemy import inspect
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def test_new_tables_created(tmp_path):
    e = _engine(tmp_path)
    names = set(inspect(e).get_table_names())
    assert {"transit_station", "transit_section_link"} <= names


def test_transit_station_columns(tmp_path):
    e = _engine(tmp_path)
    cols = {c["name"] for c in inspect(e).get_columns("transit_station")}
    assert {"line_name", "nor_code", "direction", "level_id",
            "level_name", "longitude", "latitude", "created_at"} <= cols


def test_transit_section_link_columns(tmp_path):
    e = _engine(tmp_path)
    cols = {c["name"] for c in inspect(e).get_columns("transit_section_link")}
    assert {"line_name", "nor_code", "direction", "from_level_id", "to_level_id",
            "seq", "link_id", "length_m", "pct", "built_at"} <= cols
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/db/test_section_link.py -v`
Expected: FAIL — tables not found / columns missing.

- [ ] **Step 3: Write minimal implementation**

Append to `amap_service/db/schema.py` (after the existing `transit_segment` table). Note `Float` and `BigInteger` are already imported at the top of the file:

```python
# v2 站间路况分段：站级静态数据（由 transit-build 从线路实体落地）。
transit_station = Table(
    "transit_station", metadata,
    Column("id", Integer, primary_key=True, autoincrement=True),
    Column("line_name", Text, nullable=False),
    Column("nor_code", Text),
    Column("direction", Integer, nullable=False),     # 0=上行/单环, 1=下行
    Column("level_id", Integer, nullable=False),       # 站级号
    Column("level_name", Text),                        # 站名
    Column("longitude", Float, nullable=False),        # Lon02
    Column("latitude", Float, nullable=False),         # Lat02
    Column("created_at", TIMESTAMP, server_default=func.current_timestamp()),
    UniqueConstraint("line_name", "direction", "level_id", name="idx_transit_station_uniq"),
    Index("idx_transit_station_line", "line_name", "direction"),
)

# v2 站间路况分段：相邻两站之间各路段的长度占比（静态，由 section-build 计算；不含路况）。
transit_section_link = Table(
    "transit_section_link", metadata,
    Column("id", Integer, primary_key=True, autoincrement=True),
    Column("line_name", Text, nullable=False),
    Column("nor_code", Text),
    Column("direction", Integer, nullable=False),
    Column("from_level_id", Integer, nullable=False),  # 区间起点站级
    Column("to_level_id", Integer, nullable=False),    # 区间终点站级（方法一入参）
    Column("seq", Integer, nullable=False),            # link 在区间内的顺序（0 起）
    Column("link_id", BigInteger, nullable=False),     # 路段 ID（64 位）
    Column("length_m", Float, nullable=False),         # 区间内该 link 的长度（米，事实源）
    Column("pct", Integer, nullable=False),            # 预计算占比（区间内和=100）
    Column("built_at", TIMESTAMP, server_default=func.current_timestamp()),
    UniqueConstraint("line_name", "direction", "to_level_id", "seq", name="idx_transit_section_link_uniq"),
    Index("idx_transit_section_link_line", "line_name", "direction"),
)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/db/test_section_link.py -v`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add amap_service/db/schema.py tests/db/test_section_link.py
git commit -m "feat(db): 新增 transit_station / transit_section_link 表"
```

---

## Task 2: 两个 replace 仓储函数

**Files:**
- Modify: `amap_service/db/repositories.py`
- Test: `tests/db/test_transit_station.py`（新）

- [ ] **Step 1: Write the failing test**

```python
# tests/db/test_transit_station.py
from sqlalchemy import func, select
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db
from amap_service.db.schema import transit_station, transit_section_link
from amap_service.db.repositories import replace_transit_stations, replace_transit_section_links


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def test_replace_stations_inserts_and_replaces(tmp_path):
    e = _engine(tmp_path)
    n = replace_transit_stations(e, "47", 0, "004700", [
        {"level_id": 1, "level_name": "A", "longitude": 121.4, "latitude": 31.1},
        {"level_id": 2, "level_name": "B", "longitude": 121.5, "latitude": 31.2},
    ])
    assert n == 2
    replace_transit_stations(e, "47", 0, "004700", [
        {"level_id": 1, "level_name": "A2", "longitude": 121.41, "latitude": 31.11}])  # 替换 dir0
    replace_transit_stations(e, "47", 1, "004700", [
        {"level_id": 1, "level_name": "C", "longitude": 121.6, "latitude": 31.3}])    # 另一方向不动
    with e.connect() as c:
        assert c.execute(select(func.count()).select_from(transit_station)
                         .where(transit_station.c.direction == 0)).scalar() == 1
        assert c.execute(select(func.count()).select_from(transit_station)
                         .where(transit_station.c.direction == 1)).scalar() == 1
        row = c.execute(select(transit_station.c.level_name, transit_station.c.longitude)
                        .where((transit_station.c.line_name == "47") & (transit_station.c.direction == 0))).one()
        assert row.level_name == "A2"


def test_replace_section_links_ordered_and_idempotent(tmp_path):
    e = _engine(tmp_path)
    n = replace_transit_section_links(e, "47", 0, "004700", [
        {"from_level_id": 1, "to_level_id": 2, "seq": 0, "link_id": 5130091959790075998,
         "length_m": 50.0, "pct": 60},
        {"from_level_id": 1, "to_level_id": 2, "seq": 1, "link_id": 123, "length_m": 33.3, "pct": 40},
    ])
    assert n == 2
    with e.connect() as c:
        rows = c.execute(
            select(transit_section_link.c.seq, transit_section_link.c.link_id,
                   transit_section_link.c.length_m, transit_section_link.c.pct)
            .where((transit_section_link.c.line_name == "47") & (transit_section_link.c.direction == 0))
            .order_by(transit_section_link.c.seq)
        ).all()
    assert [(r.seq, r.link_id, r.pct) for r in rows] == [(0, 5130091959790075998, 60), (1, 123, 40)]
    # 重跑同方向整体替换
    replace_transit_section_links(e, "47", 0, "004700", [
        {"from_level_id": 2, "to_level_id": 3, "seq": 0, "link_id": 9, "length_m": 10.0, "pct": 100}])
    with e.connect() as c:
        assert c.execute(select(func.count()).select_from(transit_section_link)
                         .where(transit_section_link.c.direction == 0)).scalar() == 1
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/db/test_transit_station.py -v`
Expected: FAIL — cannot import name 'replace_transit_stations'.

- [ ] **Step 3: Write minimal implementation**

Append to `amap_service/db/repositories.py`. The file already imports `Engine`, `delete`, `insert` (or uses `engine.begin()` + table inserts) — match the existing `replace_transit_segments` style. Reference how `replace_transit_segments` (near the bottom of the file) does delete+insert in one `with engine.begin() as conn:` block, and mirror it. Add these functions:

```python
def replace_transit_stations(engine: Engine, line_name: str, direction, nor_code,
                             stations: list) -> int:
    """整体替换某线路某方向的站级静态数据（先删后插，单事务）。
    stations: [{level_id, level_name, longitude, latitude}, ...]。返回写入条数。"""
    with engine.begin() as conn:
        conn.execute(
            transit_station.delete().where(
                (transit_station.c.line_name == line_name)
                & (transit_station.c.direction == direction)
            )
        )
        if not stations:
            return 0
        conn.execute(transit_station.insert(), [
            {"line_name": line_name, "nor_code": nor_code, "direction": direction,
             "level_id": s["level_id"], "level_name": s.get("level_name"),
             "longitude": s["longitude"], "latitude": s["latitude"]}
            for s in stations
        ])
        return len(stations)


def replace_transit_section_links(engine: Engine, line_name: str, direction, nor_code,
                                  rows: list) -> int:
    """整体替换某线路某方向的站间路段占比（先删后插，单事务）。
    rows: [{from_level_id, to_level_id, seq, link_id, length_m, pct}, ...]。返回写入条数。"""
    with engine.begin() as conn:
        conn.execute(
            transit_section_link.delete().where(
                (transit_section_link.c.line_name == line_name)
                & (transit_section_link.c.direction == direction)
            )
        )
        if not rows:
            return 0
        conn.execute(transit_section_link.insert(), [
            {"line_name": line_name, "nor_code": nor_code, "direction": direction,
             "from_level_id": r["from_level_id"], "to_level_id": r["to_level_id"],
             "seq": r["seq"], "link_id": r["link_id"],
             "length_m": r["length_m"], "pct": r["pct"]}
            for r in rows
        ])
        return len(rows)
```

Also ensure the import line at the top of `repositories.py` includes the new tables. Find the existing import of schema tables (e.g. `from amap_service.db.schema import road_link, road_link_coord, traffic_status, transit_line_raw, transit_segment`) and add `transit_station, transit_section_link` to it.

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/db/test_transit_station.py -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add amap_service/db/repositories.py tests/db/test_transit_station.py
git commit -m "feat(db): replace_transit_stations / replace_transit_section_links 仓储"
```

---

## Task 3: parse_line_stations

**Files:**
- Modify: `amap_service/parsing/transit.py`
- Test: `tests/parsing/test_transit.py`

- [ ] **Step 1: Write the failing test**

```python
# append to tests/parsing/test_transit.py
from amap_service.parsing.transit import parse_line_stations


def test_parse_line_stations_both_directions():
    raw = {"Data": {"LineName": "192", "NorCode": "019200",
                    "UpObject": {"UpDown": 0, "Stations": [
                        {"LevelId": 1, "LevelName": "A", "Lon02": 121.42, "Lat02": 31.13},
                        {"LevelId": 2, "LevelName": "B", "Lon02": 121.44, "Lat02": 31.16}]},
                    "DownObject": {"UpDown": 1, "Stations": [
                        {"LevelId": 1, "LevelName": "C", "Lon02": 121.5, "Lat02": 31.2}]}}}
    out = parse_line_stations(raw)
    assert [(d["line_name"], d["direction"], len(d["stations"])) for d in out] == [
        ("192", 0, 2), ("192", 1, 1)]
    assert out[0]["nor_code"] == "019200"
    assert out[0]["stations"][0] == {"level_id": 1, "level_name": "A",
                                     "longitude": 121.42, "latitude": 31.13}


def test_parse_line_stations_skips_incomplete_and_missing_dir():
    raw = {"Data": {"LineName": "X", "UpObject": {"Stations": [
        {"LevelId": 1, "LevelName": "A", "Lon02": None, "Lat02": 31.1},   # 缺坐标 -> 丢
        {"LevelId": 2, "LevelName": "B", "Lon02": 121.4, "Lat02": 31.1}]},
        "DownObject": None}}
    out = parse_line_stations(raw)
    assert len(out) == 1 and out[0]["direction"] == 0
    assert [s["level_id"] for s in out[0]["stations"]] == [2]


def test_parse_line_stations_empty():
    assert parse_line_stations({"Data": {}}) == []
    assert parse_line_stations({}) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/parsing/test_transit.py -k parse_line_stations -v`
Expected: FAIL — cannot import name 'parse_line_stations'.

- [ ] **Step 3: Write minimal implementation**

Append to `amap_service/parsing/transit.py`:

```python
def parse_line_stations(raw) -> list:
    """从 GetRoadLineEntity 响应，按方向产出站级静态数据。

    形如 {"Data": {"LineName","NorCode","UpObject":{"UpDown":0,"Stations":[{LevelId,LevelName,
    Lon02,Lat02},...]}, "DownObject":{...}|None}}。返回
    [{line_name, nor_code, direction, stations:[{level_id, level_name, longitude, latitude}]}, ...]；
    缺坐标的站点丢弃，缺方向对象跳过。"""
    data = raw.get("Data") if isinstance(raw, dict) else None
    if not isinstance(data, dict):
        return []
    line_name = str(data.get("LineName") or "")
    nor_code = data.get("NorCode")
    out = []
    for obj in (data.get("UpObject"), data.get("DownObject")):
        if not isinstance(obj, dict):
            continue
        direction = obj.get("UpDown")
        if direction is None:
            continue
        stations = []
        for s in obj.get("Stations") or []:
            if not isinstance(s, dict):
                continue
            lvl, lng, lat = s.get("LevelId"), s.get("Lon02"), s.get("Lat02")
            if lvl is None or lng is None or lat is None:
                continue
            stations.append({"level_id": lvl, "level_name": s.get("LevelName"),
                             "longitude": lng, "latitude": lat})
        out.append({"line_name": line_name, "nor_code": nor_code,
                    "direction": direction, "stations": stations})
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/parsing/test_transit.py -k parse_line_stations -v`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add amap_service/parsing/transit.py tests/parsing/test_transit.py
git commit -m "feat(parsing): parse_line_stations 按方向解析站级静态数据"
```

---

## Task 4: sdk/section_compute.py（迁纯几何 + compute_section_rows）

把 v1 `station_traffic.py` 里的纯几何函数迁到新模块，并新增 `compute_section_rows`。`station_traffic.py` 改为从新模块 re-export 这些函数（v1 的 `StationTrafficResolver` 暂不动，确保现有 `tests/sdk/test_station_traffic.py` 仍通过）。

**Files:**
- Create: `amap_service/sdk/section_compute.py`
- Modify: `amap_service/sdk/station_traffic.py`（用 re-export 替换纯函数定义，保留 `StationTrafficResolver`）
- Create: `tests/sdk/test_section_compute.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/sdk/test_section_compute.py
from amap_service.sdk.section_compute import (
    build_chain, section_links, largest_remainder, sample_chain, align_stations,
    compute_section_rows,
)


def _seg(link_id, track):
    return {"link_id": link_id, "line_track": track}


def test_build_chain_and_section_links():
    chain = build_chain([_seg(1, "0.0,0.0;0.001,0.0"), _seg(2, "0.001,0.0;0.002,0.0")])
    assert [c.link_id for c in chain] == [1, 2]
    L = chain[0].arc_end
    pairs = section_links(chain, L * 0.5, L * 1.5)
    assert [lid for lid, _ in pairs] == [1, 2]


def test_largest_remainder_sums_to_100():
    assert sum(largest_remainder([1.0, 1.0, 1.0])) == 100
    assert largest_remainder([1.0, 1.0, 1.0]) == [33, 33, 34]


def test_align_stations_out_and_back_disambiguates():
    out = "0.0,0.0;0.0025,0.0;0.005,0.0;0.0075,0.0;0.01,0.0"
    back = "0.01,0.0;0.0075,0.0;0.005,0.0;0.0025,0.0;0.0,0.0"
    chain = build_chain([_seg(1, out), _seg(1, back)])
    total = chain[-1].arc_end
    arcs = align_stations(sample_chain(chain, 20.0),
                          [(0.002, 0.0), (0.0095, 0.0), (0.005, 0.0), (0.001, 0.0)])
    assert arcs == sorted(arcs)
    assert arcs[0] < total / 2 and arcs[2] > total / 2 and arcs[3] > total / 2


def test_compute_section_rows_basic():
    chain = build_chain([_seg(1, "0.0,0.0;0.001,0.0"), _seg(2, "0.001,0.0;0.002,0.0")])
    stations = [(1, 0.0, 0.0), (2, 0.001, 0.0), (3, 0.002, 0.0)]  # (level_id, lng, lat)
    rows = compute_section_rows(chain, stations, sample_step_m=4.0)
    # 两个区间：1->2 (整条 link1), 2->3 (整条 link2)
    spans = {(r["from_level_id"], r["to_level_id"]) for r in rows}
    assert spans == {(1, 2), (2, 3)}
    for span in [(1, 2), (2, 3)]:
        seg = [r for r in rows if (r["from_level_id"], r["to_level_id"]) == span]
        assert sum(r["pct"] for r in seg) == 100
        assert [r["seq"] for r in seg] == list(range(len(seg)))
    s12 = [r for r in rows if (r["from_level_id"], r["to_level_id"]) == (1, 2)]
    assert s12 == [{"from_level_id": 1, "to_level_id": 2, "seq": 0, "link_id": 1,
                    "length_m": s12[0]["length_m"], "pct": 100}]
    assert s12[0]["length_m"] > 0


def test_compute_section_rows_too_few_stations():
    chain = build_chain([_seg(1, "0.0,0.0;0.001,0.0")])
    assert compute_section_rows(chain, [(1, 0.0, 0.0)], 4.0) == []
    assert compute_section_rows([], [(1, 0.0, 0.0), (2, 0.001, 0.0)], 4.0) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/sdk/test_section_compute.py -v`
Expected: FAIL — module `section_compute` not found.

- [ ] **Step 3: Write minimal implementation**

3a. First, look at the current `amap_service/sdk/station_traffic.py` and identify the pure-function block: `import math`, the `ChainLink` dataclass, `build_chain`, `_link_at`, `section_links`, `largest_remainder`, `sample_chain`, `align_stations`. Move ALL of them verbatim into a new file `amap_service/sdk/section_compute.py` with this header and append `compute_section_rows`:

```python
"""站间路段分解的纯几何算法（无 DB、无路况）。由 section-build 调用，结果落 transit_section_link。

坐标 (经度, 纬度)，与 geometry 一致。
"""
import math
from dataclasses import dataclass

from amap_service.sdk import geometry

_EPS_M = 1e-6

# ↓↓↓ 把 station_traffic.py 中的以下定义原样迁来：
# ChainLink, build_chain, _link_at, section_links, largest_remainder, sample_chain, align_stations
# （它们都是纯函数，逐字复制，保持实现不变）
```

After moving those, append the new function:

```python
def compute_section_rows(chain: list[ChainLink], stations: list[tuple[int, float, float]],
                         sample_step_m: float = 4.0) -> list[dict]:
    """把方向的链 + 有序站点(按 level_id 升序的 (level_id, lng, lat))算成站间路段占比行。
    -> [{from_level_id, to_level_id, seq, link_id, length_m, pct}, ...]（无路况）。"""
    if len(stations) < 2 or not chain:
        return []
    arcs = align_stations(sample_chain(chain, sample_step_m),
                          [(lng, lat) for _, lng, lat in stations])
    rows: list[dict] = []
    for idx in range(1, len(stations)):
        from_lvl, to_lvl = stations[idx - 1][0], stations[idx][0]
        pairs = section_links(chain, arcs[idx - 1], arcs[idx])
        pcts = largest_remainder([length_m for _, length_m in pairs])
        for seq, ((link_id, length_m), pct) in enumerate(zip(pairs, pcts)):
            rows.append({"from_level_id": from_lvl, "to_level_id": to_lvl, "seq": seq,
                         "link_id": link_id, "length_m": length_m, "pct": pct})
    return rows
```

3b. In `amap_service/sdk/station_traffic.py`, DELETE the moved pure-function definitions (and the now-unused `import math` / `from dataclasses import dataclass`) and replace them with a re-export so the existing `StationTrafficResolver` and v1 tests keep working. The top of `station_traffic.py` should import what the resolver needs from the new module:

```python
from amap_service.sdk.section_compute import (
    ChainLink, build_chain, section_links, largest_remainder, sample_chain, align_stations,
)
```

Keep `_data_of`, `_line_name_of`, `_stations_for`, and the `StationTrafficResolver` class exactly as they are for now (they get rewritten in Task 7). Keep the `from sqlalchemy ...` and `from amap_service.db.schema ...` imports the resolver uses. The geometry import (`from amap_service.sdk import geometry`) may stay if still referenced, otherwise remove it.

- [ ] **Step 4: Run tests to verify they pass**

Run: `.venv/bin/python -m pytest tests/sdk/test_section_compute.py -v`
Expected: PASS (5 tests).
Run: `.venv/bin/python -m pytest tests/sdk/test_station_traffic.py -q`
Expected: still PASS (v1 tests unaffected by the re-export).
Run: `.venv/bin/python -m pytest -q`
Expected: full suite PASS.

- [ ] **Step 5: Commit**

```bash
git add amap_service/sdk/section_compute.py amap_service/sdk/station_traffic.py tests/sdk/test_section_compute.py
git commit -m "refactor(sdk): 纯几何迁入 section_compute + compute_section_rows"
```

---

## Task 5: pipelines/section_build.py + 配置项

**Files:**
- Modify: `amap_service/config/schema.py`（+ `section_sample_step_m`）
- Create: `amap_service/pipelines/section_build.py`
- Test: `tests/pipelines/test_section_build.py`（新）

- [ ] **Step 1: Write the failing test**

```python
# tests/pipelines/test_section_build.py
from sqlalchemy import select
from amap_service.config.loader import load_config
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db
from amap_service.db.schema import transit_section_link
from amap_service.db.repositories import replace_transit_segments, replace_transit_stations
from amap_service.pipelines.section_build import run_section_build


class _Sdk:
    section_sample_step_m = 4.0


class _Cfg:
    sdk = _Sdk()


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def test_section_build_end_to_end(tmp_path):
    e = _engine(tmp_path)
    replace_transit_segments(e, "T1", 0, "00T1", [
        {"link_id": 1, "reverse_coords": 0, "line_track": "0.0,0.0;0.001,0.0"},
        {"link_id": 2, "reverse_coords": 0, "line_track": "0.001,0.0;0.002,0.0"},
    ])
    replace_transit_stations(e, "T1", 0, "00T1", [
        {"level_id": 1, "level_name": "A", "longitude": 0.0, "latitude": 0.0},
        {"level_id": 2, "level_name": "B", "longitude": 0.001, "latitude": 0.0},
        {"level_id": 3, "level_name": "C", "longitude": 0.002, "latitude": 0.0},
    ])
    stats = run_section_build(e, _Cfg())
    assert stats["directions"] == 1 and stats["sections"] >= 1
    with e.connect() as c:
        rows = c.execute(
            select(transit_section_link.c.to_level_id, transit_section_link.c.link_id,
                   transit_section_link.c.pct)
            .where(transit_section_link.c.line_name == "T1")
            .order_by(transit_section_link.c.to_level_id, transit_section_link.c.seq)
        ).all()
    # 区间 2 -> link1 100%；区间 3 -> link2 100%
    assert (2, 1, 100) in [(r.to_level_id, r.link_id, r.pct) for r in rows]
    assert (3, 2, 100) in [(r.to_level_id, r.link_id, r.pct) for r in rows]


def test_section_build_skips_direction_without_stations(tmp_path):
    e = _engine(tmp_path)
    replace_transit_segments(e, "T1", 0, None, [
        {"link_id": 1, "reverse_coords": 0, "line_track": "0.0,0.0;0.001,0.0"}])
    # 没有 transit_station -> 该方向无站点 -> 不产出
    stats = run_section_build(e, _Cfg())
    with e.connect() as c:
        cnt = c.execute(select(transit_section_link.c.id)).all()
    assert cnt == []
    assert stats["sections"] == 0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/pipelines/test_section_build.py -v`
Expected: FAIL — module `section_build` not found.

- [ ] **Step 3: Write minimal implementation**

3a. Add config field. In `amap_service/config/schema.py`, inside `class SdkConfig`, append after the `max_fill_links` line:

```python
    section_sample_step_m: float = 4.0  # section-build 站点对齐采样步长（米）
```

3b. Create `amap_service/pipelines/section_build.py`:

```python
"""需求2/3：把每条线路每个方向的「站间路段占比」算出来落 transit_section_link（静态，不含路况）。

纯 DB→DB：读 transit_segment（链）+ transit_station（站坐标），跑 section_compute 几何算法，
按 (line_name, direction) 整体替换写入。依赖 transit-build 已填好 segment 与 station。
"""
import logging

from sqlalchemy import Engine, select

from amap_service.db.repositories import replace_transit_section_links
from amap_service.db.schema import transit_segment, transit_station
from amap_service.sdk.section_compute import build_chain, compute_section_rows

logger = logging.getLogger(__name__)


def run_section_build(engine: Engine, config) -> dict:
    step = config.sdk.section_sample_step_m
    stats = {"lines": 0, "directions": 0, "sections": 0}

    with engine.connect() as conn:
        pairs = conn.execute(
            select(transit_segment.c.line_name, transit_segment.c.direction,
                   transit_segment.c.nor_code).distinct()
        ).all()
    seen_lines = set()
    for line_name, direction, nor_code in pairs:
        segments = _load_segments(engine, line_name, direction)
        stations = _load_stations(engine, line_name, direction)
        chain = build_chain(segments)
        rows = compute_section_rows(chain, stations, step)
        written = replace_transit_section_links(engine, line_name, direction, nor_code, rows)
        if written:
            stats["directions"] += 1
            stats["sections"] += written
            seen_lines.add(line_name)
        logger.info("section build: %s dir %s -> %d rows", line_name, direction, written)
    stats["lines"] = len(seen_lines)
    logger.info("section build: done %s", stats)
    return stats


def _load_segments(engine: Engine, line_name: str, direction) -> list:
    with engine.connect() as conn:
        rows = conn.execute(
            select(transit_segment.c.link_id, transit_segment.c.line_track)
            .where((transit_segment.c.line_name == line_name)
                   & (transit_segment.c.direction == direction))
            .order_by(transit_segment.c.seq)
        ).all()
    return [{"link_id": r.link_id, "line_track": r.line_track} for r in rows]


def _load_stations(engine: Engine, line_name: str, direction) -> list:
    with engine.connect() as conn:
        rows = conn.execute(
            select(transit_station.c.level_id, transit_station.c.longitude,
                   transit_station.c.latitude)
            .where((transit_station.c.line_name == line_name)
                   & (transit_station.c.direction == direction))
            .order_by(transit_station.c.level_id)
        ).all()
    return [(r.level_id, r.longitude, r.latitude) for r in rows]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/pipelines/test_section_build.py -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add amap_service/config/schema.py amap_service/pipelines/section_build.py tests/pipelines/test_section_build.py
git commit -m "feat(pipeline): section-build 从 segment+station 算站间占比落库"
```

---

## Task 6: transit-build 写 station + 末尾触发 section-build

**Files:**
- Modify: `amap_service/pipelines/transit_build.py`
- Test: `tests/pipelines/test_transit_build.py`（若不存在则创建；用 fake client）

- [ ] **Step 1: Write the failing test**

先看 `tests/pipelines/` 下是否已有 `test_transit_build.py` 及其 fake client 写法；若有则在其风格上追加，若无则创建。追加这个测试（用一个最小 fake TransitClient 直接喂线路实体 JSON）：

```python
# tests/pipelines/test_transit_build.py  （若已存在则只追加本测试与所需 import）
import json
from sqlalchemy import select, func
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db
from amap_service.db.repositories import upsert_road_links
from amap_service.db.schema import transit_station, transit_section_link
from amap_service.pipelines.transit_build import run_transit_build


class _FakeTransit:
    """最小 fake：固定 token、单线路列表、固定线路实体。"""
    def __init__(self, entity):
        self._entity = entity
    def get_token(self):
        return "tok", None
    def get_line_list(self, token):
        return json.dumps([{"Roadline": "T1"}])
    def get_line_entity(self, token, name):
        return json.dumps(self._entity)


class _Transit:
    token_path = None; line_name_path = None; line_name_field = "Roadline"
    company_field = "Company"; companys = None; lines = None; line_limit = 0
    def companys_set(self): return None
    def lines_set(self): return None


class _Sdk:
    match_tolerance_m = 30.0; reverse_angle_deg = 90.0; against_track_deg = 120.0
    loop_return_m = 10.0; jut_deg = 60.0; jut_neighbor_deg = 45.0; jut_offtrack_m = 15.0
    against_window_frac = 0.2; against_window_m = 80.0; connect_gap_m = 8.0
    max_fill_links = 8; refine_passes = 1; densify_step_m = 15.0; section_sample_step_m = 4.0


class _Cfg:
    transit = _Transit(); sdk = _Sdk()


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def test_transit_build_writes_stations_and_sections(tmp_path):
    e = _engine(tmp_path)
    # 路网：一条直路 link 1，供 SDK 匹配
    upsert_road_links(e, [{"link_id": 1, "road_name": "R", "length": 1, "formway": 15,
                           "roadclass": 9, "line_track": "",
                           "coords": [(0.0, 0.0), (0.0005, 0.0), (0.001, 0.0)]}])
    entity = {"Data": {"LineName": "T1", "NorCode": "00T1", "UpObject": {
        "UpDown": 0, "LineLonLat": "0.0,0.0;0.0005,0.0;0.001,0.0",
        "Stations": [{"LevelId": 1, "LevelName": "A", "Lon02": 0.0, "Lat02": 0.0},
                     {"LevelId": 2, "LevelName": "B", "Lon02": 0.001, "Lat02": 0.0}]}}}
    run_transit_build(e, _FakeTransit(entity), _Cfg())
    with e.connect() as c:
        st = c.execute(select(func.count()).select_from(transit_station)).scalar()
        sec = c.execute(select(func.count()).select_from(transit_section_link)).scalar()
    assert st == 2          # 两个站落库
    assert sec >= 1         # 站间占比已由末尾的 section-build 产出
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/pipelines/test_transit_build.py -k stations_and_sections -v`
Expected: FAIL — `transit_station` count 0 / `transit_section_link` count 0 (stations not written, section-build not triggered).

- [ ] **Step 3: Write minimal implementation**

In `amap_service/pipelines/transit_build.py`:

3a. Add imports near the top:
```python
from amap_service.db.repositories import replace_transit_segments, replace_transit_stations
from amap_service.parsing.transit import (
    extract_line_records, parse_line_tracks, parse_line_stations, select_line_names,
)
from amap_service.pipelines.section_build import run_section_build
```
(Merge with the existing import lines for `replace_transit_segments` and the `parsing.transit` names rather than duplicating.)

3b. Inside the per-line loop, after the existing `tracks = parse_line_tracks(...)` handling, also write stations. Locate the block that iterates `for t in tracks:` and, right before or after it (per line, once), add:
```python
            for st in parse_line_stations(json.loads(raw_entity)):
                replace_transit_stations(
                    engine, st["line_name"], st["direction"], st["nor_code"], st["stations"]
                )
```
Note: `raw_entity` is already parsed once for `tracks`; reuse a single `parsed = json.loads(raw_entity)` if convenient to avoid double-parsing — if the existing code does `parse_line_tracks(json.loads(raw_entity))`, refactor to:
```python
            parsed = json.loads(raw_entity)
            tracks = parse_line_tracks(parsed)
            ...
            for st in parse_line_stations(parsed):
                replace_transit_stations(engine, st["line_name"], st["direction"],
                                         st["nor_code"], st["stations"])
```

3c. After the whole `for name in to_process:` loop completes (just before the final `logger.info("transit build: done ...")` / `return stats`), trigger section-build:
```python
    try:
        sec = run_section_build(engine, config)
        stats["sections"] = sec.get("sections", 0)
    except Exception:  # noqa: BLE001 - section-build failure must not fail the whole transit build
        logger.exception("transit build: section-build step failed")
    return stats
```
Add `"sections": 0` to the initial `stats = {...}` dict so the key always exists.

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/pipelines/test_transit_build.py -v`
Expected: PASS.
Run: `.venv/bin/python -m pytest -q`
Expected: full suite PASS.

- [ ] **Step 5: Commit**

```bash
git add amap_service/pipelines/transit_build.py tests/pipelines/test_transit_build.py
git commit -m "feat(pipeline): transit-build 落 transit_station 并末尾触发 section-build"
```

---

## Task 7: 重写 StationTrafficResolver 为读取器

把 `station_traffic.py` 改成「线名进、查 `transit_section_link` + `traffic_status`」，删除其中的 re-export 与 v1 几何相关残留；重写 `tests/sdk/test_station_traffic.py` 为基于表的测试。

**Files:**
- Modify: `amap_service/sdk/station_traffic.py`（整体重写为读取器）
- Modify: `amap_service/sdk/__init__.py`
- Rewrite: `tests/sdk/test_station_traffic.py`

- [ ] **Step 1: Rewrite the test (the new failing spec)**

把 `tests/sdk/test_station_traffic.py` 整个文件替换为：

```python
from amap_service.config.schema import DatabaseConfig, SqliteConfig
from amap_service.db.engine import make_engine
from amap_service.db.migrate import init_db
from amap_service.db.repositories import replace_transit_section_links, upsert_traffic_status
from amap_service.sdk import StationTrafficResolver
from amap_service.sdk.station_traffic import StationTrafficResolver as DirectResolver


def _engine(tmp_path):
    e = make_engine(DatabaseConfig(type="sqlite", sqlite=SqliteConfig(path=str(tmp_path / "t.db"))))
    init_db(e)
    return e


def _seed(e):
    replace_transit_section_links(e, "T1", 0, "00T1", [
        {"from_level_id": 1, "to_level_id": 2, "seq": 0, "link_id": 1, "length_m": 60.0, "pct": 60},
        {"from_level_id": 1, "to_level_id": 2, "seq": 1, "link_id": 2, "length_m": 40.0, "pct": 40},
        {"from_level_id": 2, "to_level_id": 3, "seq": 0, "link_id": 3, "length_m": 10.0, "pct": 100},
    ])
    upsert_traffic_status(e, [{"link_id": 1, "state": 3}, {"link_id": 2, "state": 2}])
    # link 3 无路况 -> 默认 1


def test_station_section_reads_table_and_overlays_traffic(tmp_path):
    e = _engine(tmp_path)
    _seed(e)
    r = StationTrafficResolver(e)
    assert r.station_section("T1", 0, 2) == [
        {"link_id": 1, "state": 3, "pct": 60},
        {"link_id": 2, "state": 2, "pct": 40},
    ]
    assert r.station_section("T1", 0, 3) == [{"link_id": 3, "state": 1, "pct": 100}]  # 缺省 1


def test_station_section_empty_when_absent(tmp_path):
    e = _engine(tmp_path)
    r = StationTrafficResolver(e)
    assert r.station_section("T1", 0, 2) == []      # 未 build
    _seed(e)
    assert r.station_section("T1", 0, 99) == []      # 无该区间
    assert r.station_section("NOPE", 0, 2) == []     # 无该线路


def test_line_sections_shape_and_traffic(tmp_path):
    e = _engine(tmp_path)
    _seed(e)
    out = StationTrafficResolver(e).line_sections("T1")
    assert set(out.keys()) == {0}
    dir0 = out[0]
    assert [list(d.keys())[0] for d in dir0] == [2, 3]
    assert dir0[0][2] == [{"link_id": 1, "state": 3, "pct": 60},
                          {"link_id": 2, "state": 2, "pct": 40}]
    for d in dir0:
        assert sum(x["pct"] for x in list(d.values())[0]) == 100


def test_line_sections_empty_when_absent(tmp_path):
    e = _engine(tmp_path)
    assert StationTrafficResolver(e).line_sections("T1") == {}


def test_resolver_exported():
    assert StationTrafficResolver is DirectResolver
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/sdk/test_station_traffic.py -v`
Expected: FAIL — `station_section("T1", 0, 2)` signature/behavior mismatch (v1 took a line_object dict).

- [ ] **Step 3: Rewrite the implementation**

Replace the ENTIRE contents of `amap_service/sdk/station_traffic.py` with the reader:

```python
"""查询层 SDK：给定线路名称，返回相邻两站之间各路段的路况(state)与长度占比(pct)。

读静态表 transit_section_link（由 section-build 预计算）+ 实时表 traffic_status，
不跑任何几何。路况缺失默认 default_state(1)。
"""
from sqlalchemy import Engine, select

from amap_service.db.schema import traffic_status, transit_section_link


class StationTrafficResolver:
    def __init__(self, engine: Engine, default_state: int = 1):
        self.engine = engine
        self.default_state = default_state

    def station_section(self, line_name: str, direction: int, level_id: int) -> list[dict]:
        """方法一：站 (level_id 的上一站) -> 站 level_id 之间的路段列表。
        -> [{"link_id", "state", "pct"}, ...]，pct 之和=100；无数据返回 []。"""
        with self.engine.connect() as conn:
            rows = conn.execute(
                select(transit_section_link.c.link_id, transit_section_link.c.pct)
                .where((transit_section_link.c.line_name == line_name)
                       & (transit_section_link.c.direction == direction)
                       & (transit_section_link.c.to_level_id == level_id))
                .order_by(transit_section_link.c.seq)
            ).all()
        if not rows:
            return []
        traffic = self._load_traffic([r.link_id for r in rows])
        return [{"link_id": r.link_id, "state": traffic.get(r.link_id, self.default_state),
                 "pct": r.pct} for r in rows]

    def line_sections(self, line_name: str) -> dict:
        """方法二：整条线路所有方向、所有站间区间。
        -> { direction: [ {to_level_id: [ {link_id, state, pct}, ... ]}, ... ] }；无数据返回 {}。"""
        with self.engine.connect() as conn:
            rows = conn.execute(
                select(transit_section_link.c.direction, transit_section_link.c.to_level_id,
                       transit_section_link.c.seq, transit_section_link.c.link_id,
                       transit_section_link.c.pct)
                .where(transit_section_link.c.line_name == line_name)
                .order_by(transit_section_link.c.direction, transit_section_link.c.to_level_id,
                          transit_section_link.c.seq)
            ).all()
        if not rows:
            return {}
        traffic = self._load_traffic([r.link_id for r in rows])
        result: dict = {}
        for r in rows:
            dir_list = result.setdefault(r.direction, [])
            if not dir_list or list(dir_list[-1].keys())[0] != r.to_level_id:
                dir_list.append({r.to_level_id: []})
            dir_list[-1][r.to_level_id].append(
                {"link_id": r.link_id, "state": traffic.get(r.link_id, self.default_state),
                 "pct": r.pct})
        return result

    def _load_traffic(self, link_ids: list) -> dict:
        if not link_ids:
            return {}
        with self.engine.connect() as conn:
            rows = conn.execute(
                select(traffic_status.c.link_id, traffic_status.c.state)
                .where(traffic_status.c.link_id.in_(set(link_ids)))
            ).all()
        return {r.link_id: r.state for r in rows}
```

3b. Replace `amap_service/sdk/__init__.py` with:
```python
"""Requirement 3 SDK: GPS track ↔ road-link conversion + 站间路况分段(查询层)。"""
from amap_service.sdk.conversion import LinkInfo, TrackConverter
from amap_service.sdk.station_traffic import StationTrafficResolver

__all__ = ["LinkInfo", "TrackConverter", "StationTrafficResolver"]
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `.venv/bin/python -m pytest tests/sdk/test_station_traffic.py -v`
Expected: PASS (5 tests).
Run: `.venv/bin/python -m pytest -q`
Expected: full suite PASS (section_compute tests still cover the geometry; nothing imports the removed v1 helpers).

If the full suite reports an import error from a leftover reference to `_stations_for` / `build_chain` etc. via `amap_service.sdk.station_traffic`, that means another module imported them from the old location — grep `grep -rn "station_traffic import" amap_service tests` and repoint those imports to `amap_service.sdk.section_compute`. (Expected: only `section_build.py` imports from `section_compute`, and tests import correctly — so no leftovers — but verify.)

- [ ] **Step 5: Commit**

```bash
git add amap_service/sdk/station_traffic.py amap_service/sdk/__init__.py tests/sdk/test_station_traffic.py
git commit -m "refactor(sdk): StationTrafficResolver 改为查表读取器(线名进+叠路况)"
```

---

## Task 8: CLI run-once section-build

**Files:**
- Modify: `amap_service/cli.py`
- Test: `tests/test_cli.py`

- [ ] **Step 1: Write the failing test**

先看 `tests/test_cli.py` 现有写法（它如何对 `cmd_run_once` 或 `main` 打桩/调用各 job）。在其风格上追加一个断言 `section-build` 被接受并分发到 `run_section_build` 的测试。若现有测试用 monkeypatch 替换 pipeline 函数，照此模式：

```python
# append to tests/test_cli.py  （依据文件已有的 monkeypatch/桩风格调整）
def test_run_once_section_build_dispatches(monkeypatch, tmp_path):
    import amap_service.cli as cli
    called = {}
    monkeypatch.setattr(cli, "run_section_build", lambda engine, config: called.setdefault("ok", True) or {"sections": 0})
    # 复用本文件已有的 config 加载/桩方式构造一个最小可运行环境；
    # 关键断言：job="section-build" 不再抛 "unknown job"，并调用了 run_section_build
    cli.cmd_run_once(_test_config_path(monkeypatch, tmp_path), "section-build")
    assert called.get("ok") is True
```

注意：`_test_config_path` 仅示意——请改用 `tests/test_cli.py` 中已有的配置构造辅助；若没有，则参考其它 pipeline 测试用 `load_config` + 临时 config 的方式。本步目标只是验证 `section-build` 这个 job 被接受并调用 `run_section_build`。

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/test_cli.py -k section_build -v`
Expected: FAIL — `unknown job: section-build` (SystemExit) 或 `run_section_build` 未被调用。

- [ ] **Step 3: Write minimal implementation**

In `amap_service/cli.py`:

3a. Add import with the other pipeline imports:
```python
from amap_service.pipelines.section_build import run_section_build
```

3b. In `cmd_run_once`, add a branch (after the transit branch, before the final `raise SystemExit`):
```python
    if job == "section-build":
        return run_section_build(engine, config)
```

3c. In `main`, add `"section-build"` to the run-once choices:
```python
    ro.add_argument("job", choices=["road-network", "traffic", "transit", "transit-build", "section-build"])
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/test_cli.py -k section_build -v`
Expected: PASS.
Run: `.venv/bin/python -m pytest tests/test_cli.py -q`
Expected: all CLI tests PASS.

- [ ] **Step 5: Commit**

```bash
git add amap_service/cli.py tests/test_cli.py
git commit -m "feat(cli): run-once section-build 子命令"
```

---

## Task 9: 配置样例 + 全量回归 + 文档落地说明

**Files:**
- Modify: `config/config.yaml.example`
- Modify: `docs/命令说明.md`（若存在；记录 section-build 命令）

- [ ] **Step 1: 更新配置样例**

在 `config/config.yaml.example` 的 `sdk:` 块下，追加一行（与现有 sdk 字段同缩进）：
```yaml
  section_sample_step_m: 4.0   # section-build 站点对齐采样步长（米）
```
若 `config/config.yaml` 也在版本控制内且含 sdk 块，同样追加（不破坏现有值）。

- [ ] **Step 2: 文档**

在 `docs/命令说明.md` 增加一节（若文件存在）：
```markdown
## section-build（站间路段占比）

从已落库的 transit_segment + transit_station 计算每条线路每个方向的「站间路段长度占比」，
写入 transit_section_link（静态，不含路况）。transit-build 末尾会自动跑一次；也可单独：

​```bash
python -m amap_service run-once section-build
​```

查询层 SDK（StationTrafficResolver）据此 + 实时 traffic_status 给前端返回 {link_id, state, pct}。
```

- [ ] **Step 3: 全量回归**

Run: `.venv/bin/python -m pytest -q`
Expected: 全部通过。

- [ ] **Step 4: Commit**

```bash
git add config/config.yaml.example docs/命令说明.md
git commit -m "docs+config: section-build 配置样例与命令说明"
```

---

## Self-Review

**1. Spec coverage**
- §3.1 transit_station 表 → Task 1。§3.2 transit_section_link 表 → Task 1。
- §4 仓储 replace_* → Task 2；parse_line_stations → Task 3；section_compute + compute_section_rows → Task 4；section_build 管线 → Task 5；transit_build 写 station + 触发 → Task 6；station_traffic 读取器 → Task 7；CLI → Task 8；config 字段 → Task 5（schema）+ Task 9（样例）。
- §5 查询层契约（线名进、返回不变、空→[]/{}、缺省 state 1、一次路况 join）→ Task 7。
- §6 构建器算法（build_chain→align→section_links→largest_remainder→行）→ Task 4（compute_section_rows）+ Task 5（驱动）。
- §7 迁移（create_all 建新表、两步命令）→ Task 1（建表）+ Task 6/8（命令）+ Task 9（文档）。
- §8 测试矩阵 → 各任务对应测试齐备。
- §9 YAGNI（不存几何/不存路况/不接 Redis/不做指纹）→ 计划未引入，符合。

**2. Placeholder scan**：除 Task 6/8 明确要求"按现有 test 文件风格适配桩/辅助"（因这些测试文件的既有结构需实现者现场对齐，非占位省略）外，其余步骤均给出完整代码与确切命令、预期输出。Task 8 的 `_test_config_path` 已标注为"改用文件已有辅助"。

**3. Type consistency**：`compute_section_rows(chain, stations, sample_step_m) -> list[dict]`（行键 from_level_id/to_level_id/seq/link_id/length_m/pct）在 Task 4 定义，Task 5 `run_section_build` 透传给 `replace_transit_section_links`（Task 2，吃同样键），查询层读出 link_id/pct/to_level_id（Task 7）一致；`replace_transit_stations` 吃 {level_id, level_name, longitude, latitude}（Task 2）与 parse_line_stations 产出（Task 3）、transit_build 传入（Task 6）一致；`StationTrafficResolver(engine, default_state)` 新签名（Task 7）与其 v1 多参签名的差异已在 Task 7 整体重写中消解，且无其他调用方传 sample_step_m（section-build 用 config，不经 resolver）。
